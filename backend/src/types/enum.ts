export enum Role {
  User = 'USER',
  Admin = 'ADMIN'
}

export enum EventStatus {
  Draft = 'DRAFT',
  Published = 'PUBLISHED',
  Ongoing = 'ONGOING',
  Canceled = 'CANCELED',
  Ended = 'ENDED'
}

export enum BookingStatus {
  Paid = 'PAID',
  Canceled = 'CANCELED',
  Waiting = 'Wait',
  Expired = 'EXPIRED'
}

export enum TicketState {
  Expired = 'EXPIRED',
  Canceled = 'CANCELED',
  Available = 'AVAILABLE',
}

export enum Category {
  Music = 'Âm nhạc',
  Other = 'Khác',
  Sport = 'Thể thao',
  Art = 'Nghệ thuật'
}
